node_modules/
server/.data/
*.log
.env
.DS_Store
coverage/
*.tmp

# Large generated blocklist from `npm run update:blocklist` (regenerable).
data/blocklist.extended.json

# Note: extension/vendor/ and extension/rules/network-rules.json ARE committed
# (generated by `npm run build`) so the unpacked extension loads directly from
# a fresh clone.

# Packaged downloadables
dist/
